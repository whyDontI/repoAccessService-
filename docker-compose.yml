services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: repo_access
      POSTGRES_USER: repo_access
      POSTGRES_PASSWORD: repo_access
    ports:
      - "5432:5432"
    volumes:
      - ./backend/app/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
